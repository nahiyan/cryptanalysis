# Experiments

This directory is reserved for reproducible experiment definitions. An
experiment should identify its attack, inputs, encoder, solver configuration,
verification command, and expected outputs.

`wp-debug/` is retained as a placeholder for the existing word-propagation
debugging environment.
