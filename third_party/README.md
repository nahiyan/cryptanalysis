# Third-party source

External source that is moved or imported in future belongs here. Keep its
origin, local modifications, and build instructions with the imported project.

The existing `solvers/` tree remains at the repository root for now because it
contains an uninitialized Git submodule. It can move separately when that
submodule migration is handled explicitly.
