# Attacks

This directory contains code, constraints, starting points, and verification
material that belong to a specific cryptanalytic attack.

Organize new work first by hash function and then by attack type, for example
`sha256/collision` or `md4/preimage`.
